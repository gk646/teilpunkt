#ifndef TPUNKT_FWD_H
#define TPUNKT_FWD_H

namespace tpunkt
{
    struct User;
    struct File;
    struct Credentials;
    struct AuthToken;
    struct UserName;
    struct SessionID;
    struct UserStorage;
    struct SessionStorage;
    template<typename T>
    struct SecureContainer;
} // namespace tpunkt

#endif //TPUNKT_FWD_H